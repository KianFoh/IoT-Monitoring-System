# Install Depedency
npm install

# Start React Sever (development)
npm run dev

# Production

# Build version
npm run build

# Install Serve (run once)
npm install -g serve 

# Start Server
npm install --save-dev serve
