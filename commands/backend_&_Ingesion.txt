# Create Python Virtual Environment
python -m venv venv

# Activate Environment
venv\Scripts\activate

# Install Dependency
pip install --no-deps -r requirements.txt

# Start API from backend/
python main.py

# Start Rollup Service from rollup_service/
python main.py

# Start Ingestion Service from ingestion/
python main.py

# Backend Create first superuser scripts
python -m app.scripts.create_superadmin      
