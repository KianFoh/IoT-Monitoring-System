# Create Python Virtual Environment
python -m venv venv

# Activate Environment
venv\Scripts\activate

# Install Dependency
pip install --no-deps -r requirement.txt

# Start API & Ingesion Services
python main.py

# Backend Create first superuser scripts
python -m app.scripts.create_superadmin